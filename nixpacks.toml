[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["npm install", "npm run postinstall", "npm rebuild better-sqlite3 --prefix backend"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node backend/server.js"
