[phases.setup]
nixPkgs = ["nodePackages.npm", "nodejs-20_x", "gcc", "gnumake", "python3"]

[phases.install]
cmds = ["npm install", "npm run postinstall"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
